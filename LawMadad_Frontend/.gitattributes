# Set the default behavior to automatically normalize line endings
* text=auto

# For specific file types, enforce Unix-style line endings
*.sh text eol=lf

# For specific file types, enforce Windows-style line endings
*.bat text eol=crlf

# Mark files that should always be treated as binary
*.png binary
*.jpg binary
*.gif binary
*.zip binary

# Specify end-of-line normalization for Markdown files
*.md text eol=lf
